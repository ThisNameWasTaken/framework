(function(){'use strict';let buttons=document.getElementsByClassName('button');for(let i=0;i<buttons.length;i++){buttons[i].addEventListener('mouseup',BlurButton);buttons[i].addEventListener('mouseout',BlurButton)}
function BlurButton(){let self=this;setTimeout(function(){self.blur()},0)}})()