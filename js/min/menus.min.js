(function(){'use strict';let clickableMenus=document.getElementsByClassName('menu');for(let i=0;i<clickableMenus.length;i++){UpgradeMenu(clickableMenus[i])}
let hoverableMenu=document.getElementsByClassName('menu-hover');for(let i=0;i<hoverableMenu.length;i++){UpgradeMenu(hoverableMenu[i])}
let lastActiveMenuContent;let isMenuActive=!1;if(clickableMenus)
window.addEventListener('click',function(){if(!isMenuActive)
return;lastActiveMenuContent.classList.add('inactive');isMenuActive=!1});function UpgradeMenu(menu){let menuContent=menu.getElementsByClassName('menu__content')[0];menu.classList.add('js-pos-static');menuContent.style.width=((menuContent.scrollWidth+1)+'px');menuContent.style.height=(menuContent.scrollHeight+'px');menu.classList.remove('js-pos-static');menu.classList.add('is-upgraded');if(menu.classList.contains('menu')){menuContent.classList.add('inactive');let menuButton=menu.getElementsByClassName('menu__button')[0];menuButton.addEventListener('click',function(){if(lastActiveMenuContent===menuContent&&!menuContent.classList.contains('inactive'))
return;setTimeout(function(){menuContent.classList.remove('inactive');isMenuActive=!0;lastActiveMenuContent=menuContent},0)})}
let menuItems=menu.getElementsByClassName('menu__item');for(let i=0;i<menuItems.length;i++){menuItems[i].classList.add('ripple-effect')}}})()