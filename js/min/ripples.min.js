(function(){'use strict';let elements=document.getElementsByClassName('ripple-effect');let ignoreMouseDown=!1,ignoreMouseUp=!1;for(let i=0;i<elements.length;i++){let rippleContainer=SetupRippleEffect(elements[i]);elements[i].addEventListener('mousedown',function(){DisplayRipple(event,this,rippleContainer)});elements[i].addEventListener('mouseup',function(){FadeRipple(event,this,rippleContainer)});elements[i].addEventListener('mouseleave',function(){FadeRipple(event,this,rippleContainer)});elements[i].addEventListener('touchstart',function(){DisplayRipple(event,this,rippleContainer)});elements[i].addEventListener('touchend',function(){FadeRipple(event,this,rippleContainer)})}
function DisplayRipple(event,element,rippleContainer){if(event.type==='mousedown'){if(ignoreMouseDown){ignoreMouseDown=!1;return}}else if(event.type==='touchstart'){ignoreMouseDown=ignoreMouseUp=!0}
let ripple=rippleContainer.lastElementChild;ripple.classList.add('is-visible');SetRipplePos(event,element,rippleContainer)}
function FadeRipple(event,element,rippleContainer){if(event.type==='mouseup')
if(ignoreMouseUp){ignoreMouseUp=!1;return}
let ripple=rippleContainer.lastElementChild;if(!ripple.classList.contains('is-visible'))
return;ripple.classList.remove('is-visible');ripple.classList.add('is-fading');setTimeout(function(){rippleContainer.removeChild(ripple)},600);AddRippleToElement(element,rippleContainer)}
function SetupRippleEffect(element){let rippleContainer=document.createElement('div');rippleContainer.classList.add('ripple-container');element.appendChild(rippleContainer);AddRippleToElement(element,rippleContainer);return rippleContainer}
function AddRippleToElement(element,rippleContainer){let ripple=document.createElement('span');ripple.classList.add('ripple');ripple.style.width=ripple.style.height=Math.max(element.clientHeight,element.clientWidth)+'px';rippleContainer.appendChild(ripple)}
function SetRipplePos(event,element,rippleContainer){let offset=element.getBoundingClientRect();let x=event.clientX!==undefined?event.clientX:event.touches[0].clientX;let y=event.clientY!==undefined?event.clientY:event.touches[0].clientY;x=(x-offset.left)+'px';y=(y-offset.top)+'px';let ripple=rippleContainer.lastElementChild;ripple.style.top=y;ripple.style.left=x}})()